runners:
 local-runner:
  image: stag:5000/ubuntu-generic
  
jobs:   
 hello:
  resource_spec: micro 
  runner: local-runner
  privileged: True
  device: raspberry-pi-device1
  steps:
   - run:
      name: Setup Environment
      command: |
       apt-get -y update
       apt-get -y install python python3-pip 

   - run:
      name: Connect to Pico
      on-device: True
      command: |
       print("Hello World")
    
workflows:
 complete-build-test:
  jobs:
   - hello